# 本地开发环境变量模板
#
# 用法:复制本文件为 .env 并填入真实值(.env 已被 .gitignore 排除,不会进仓库)。
#   cp .env.example .env
#
# IntelliJ IDEA 注入方式(二选一):
#   A. Run/Debug Configurations → AiHubApplication → Environment variables
#      直接填: DB_PASSWORD=你的密码;DB_USERNAME=root
#   B. 安装 EnvFile 插件,在运行配置里勾选 Enable EnvFile 并指向本目录的 .env
#
# 这些变量对应 backend/src/main/resources/application.yml 中的占位:
#   username: ${DB_USERNAME:root}
#   password: ${DB_PASSWORD:}

# MySQL 数据库账号(默认 root)
DB_USERNAME=root

# MySQL 数据库密码(必填,否则后端启动报 Access denied)
DB_PASSWORD=your_mysql_password
