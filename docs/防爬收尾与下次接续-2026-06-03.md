# 防爬收尾与下次接续 - 2026-06-03

## 今天做到哪里

- 已完成读取接口、签名挑战、验证码、访问日志、风险降速、统一安全头、上传资源签名与分页收口这条主线。
- 资产中心已经补上“分页扫库 + 详情/下载遍历”联动识别。
- 上传资源下载已经形成三层聚类：
  - 同账号 + 同项目
  - 同账号 + 多项目
  - 同一 IP + 多账号
  - 同一 IP + 同一 UA + 多账号
- 已进一步把“同一 IP + 多账号 / 同一 IP + 同一 UA + 多账号”升级为：
  - 长窗口累计超阈值
  - 且近 2 分钟仍在持续突发下载
  - 双条件同时满足才拦截
- 已继续往 `/uploads/**` 下载风控里补上“行为节奏”识别里的第一项：
  - 同账号稳定间隔批量下载节奏
  - 默认阈值为 `2` 分钟窗口内、`900ms ~ 5000ms` 的稳定间隔、抖动不超过 `160ms`
  - 连续节奏达到 `10` 次以上、且不同资源数达到 `12` 个以上后，后续请求开始返回 `429`
- 已继续往 `/uploads/**` 下载风控里补上“行为节奏”识别里的第二项：
  - 同账号长时间低抖动的批量顺序拉取
  - 默认阈值为 `20` 分钟窗口内、`6000ms ~ 45000ms` 的低抖动间隔、抖动不超过 `1200ms`
  - 连续节奏达到 `8` 次以上、且不同资源数达到 `10` 个以上后，后续请求开始返回 `429`
- 已继续往 `/uploads/**` 下载风控里补上“行为节奏”识别里的第三项：
  - 同账号秒级 burst 批量下载
  - 默认阈值为 `3` 秒窗口内命中 `12` 个不同资源时先记一次 burst 事件
  - 若 `2` 分钟内再次出现同等级 burst，则后续请求开始返回 `429`

## 今天最后确认过的运行态

- `http://localhost:8080`
  - 已确认带上“同项目下载轨迹聚类限制”
  - 已确认带上“跨项目分散搬运聚类限制”
  - 已复核通过“长窗口累计 + 近 2 分钟持续突发”版同一 IP 多账号协同搬运限制
  - 已复核通过“长窗口累计 + 近 2 分钟持续突发”版同一 IP/UA 多账号协同搬运限制
  - 已复核通过“10 秒内同一 IP 跨多个账号访问不同资源超过 15 个”版更快协同节奏限制
  - 已复核通过“10 秒内同一 IP/UA 跨多个账号访问不同资源超过 12 个”版更快协同节奏限制
  - 已复核通过“长窗口残留但近 2 分钟无突发时，单账号下载返回 200”
  - 在 IDEA 重启后，已复核通过“稳定间隔批量下载节奏”规则：
    - 前 `12` 次稳定节奏访问返回 `200`
    - 第 `13`、`14` 次稳定节奏访问返回 `429`
    - 返回文案为：`检测到稳定间隔的批量下载节奏`
  - 已复核通过“长时间低抖动的批量顺序拉取”规则：
    - 前 `10` 次低抖动顺序访问返回 `200`
    - 第 `11`、`12` 次返回 `429`
    - 返回文案为：`检测到长时间低抖动的批量顺序拉取`
  - 已复核通过“秒级 burst”规则：
    - 第一轮 `14` 次突发访问返回 `200`
    - 第二轮第 `13`、`14` 次返回 `429`
    - 返回文案为：`检测到秒级突发批量下载行为`
- `http://localhost:18080`
  - 已额外黑盒验证“同一 IP 多账号协同搬运聚类限制（带近 2 分钟突发门槛）”
  - 已额外黑盒验证“同一 IP/UA 多账号协同搬运聚类限制（带近 2 分钟突发门槛）”
  - 已额外黑盒验证“长窗口残留但近 2 分钟无突发时，单账号正常下载返回 200”
  - 验证完成后已关闭
- `http://localhost:18080`
  - 已使用当前代码态重新拉起临时实例，并补上数据库连接环境变量
  - 已在真实 `uploads` 资源存在的前提下完成完整黑盒复核
  - 前 `12` 次稳定节奏访问返回 `200`
  - 第 `13`、`14` 次稳定节奏访问返回 `429`
  - 返回文案为：`检测到稳定间隔的批量下载节奏`
- `http://localhost:18082`
  - 已使用当前代码态重新拉起临时实例，并补上数据库连接环境变量
  - 已在真实 `uploads` 资源存在的前提下完成“长时间低抖动的批量顺序拉取”黑盒复核
  - 按约 `6.5` 秒低抖动间隔顺序访问真实签名资源时，前 `10` 次返回 `200`
  - 第 `11`、`12` 次返回 `429`
  - 返回文案为：`检测到长时间低抖动的批量顺序拉取`
- `http://localhost:18083`
  - 已使用当前代码态重新拉起临时实例，并补上数据库连接环境变量
  - 已在真实 `uploads` 资源存在的前提下完成“秒级 burst”黑盒复核
  - 第一轮 `14` 个真实签名资源的秒级突发访问全部返回 `200`
  - 第二轮同样的秒级突发访问里，第 `13`、`14` 次返回 `429`
  - 返回文案为：`检测到秒级突发批量下载行为`
## 当前代码态结论

- 防爬能力约 `9.8 / 10`
- 对普通脚本、单账号粗暴搬运、单项目慢速搬运、跨项目分散搬运、同一 IP 多账号协同搬运，已经有比较完整的约束
- 当前代码态已经带上“稳定间隔批量下载节奏”识别
- 当前代码态已经继续带上“长时间低抖动的批量顺序拉取”识别
- 当前代码态已经继续带上“秒级 burst”识别
- `18080` 与 IDEA 重启后的 `8080` 已都完成“稳定间隔批量下载节奏”完整验证
- `18082` 与 IDEA 重启后的 `8080` 已都完成“长时间低抖动的批量顺序拉取”完整验证
- `18083` 与 IDEA 重启后的 `8080` 已都完成“秒级 burst”完整验证
- IDEA 重启后的 `8080` 已继续补齐“10 秒内同一 IP / 同一 IP+UA 更快协同节奏”运行态复核

## 下次继续时第一步

1. 继续做更慢速、更分散的下载脚本识别
2. 继续观察共享办公 / NAT 场景下 `10` 秒级协同规则的误伤边界
3. 如继续新增规则，优先在临时实例黑盒验证，再回到 `8080` 复核

## 相关文档

- [当前项目防爬能力评估报告](./当前项目防爬能力评估报告.md)
- [防爬加固TODO](./防爬加固TODO.md)
