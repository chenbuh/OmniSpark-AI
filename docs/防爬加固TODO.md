# 防爬加固 TODO

下次优先完成这些项，目标是从“能挡普通脚本”提升到“能抗登录后批量爬取和 API Key 滥用”。

## 续作标记

- 记录时间：2026-06-02
- 当前状态：已完成首轮加固开发。
- 落地入口：真实 IP 解析、读取接口限流、API Key 风控、签名 challenge、访问日志、风险降速和 canary 水印已接入。
- 复查补充：2026-06-03 继续加固时已补齐“带自定义 `@RateLimit` 的 GET 接口仍需自动补 IP/USER_API 缺失维度”、同账号来源 IP/UA 风险计数、资产分页扫库与详情遍历联动识别，以及上传资源“同账号 + 同项目”“同账号 + 多项目”“同一 IP + 多账号”“同一 IP + 同一 UA + 多账号”下载轨迹聚类限制；随后又把同一 IP / 同一 IP+UA 聚类升级为“长窗口累计 + 近 2 分钟持续突发”双条件，减少旧窗口残留误伤。
- 再补充：2026-06-03 晚间已继续落地上传下载“行为节奏”识别中的第一项，即“稳定间隔批量下载节奏”；基于当前代码态重新拉起的 `18080` 临时实例已在真实 `uploads` 资源存在前提下完成完整复核，前 `12` 次稳定节奏访问返回 `200`，第 `13`、`14` 次开始返回 `429`；随后在 IDEA 重启后的 `8080` 上按同样链路复核，也已得到一致结果。
- 再补充：2026-06-03 深夜又继续落地“长时间低抖动的批量顺序拉取”识别；基于当前代码态重新拉起的 `18082` 临时实例已完成完整复核，按约 `6.5` 秒低抖动间隔顺序访问真实签名资源时，前 `10` 次返回 `200`，第 `11`、`12` 次开始返回 `429`；随后在 IDEA 重启后的 `8080` 上按同样链路复核，也已得到一致结果。
- 再补充：2026-06-03 深夜继续落地“秒级 burst”识别，并采用“先记 burst 事件、2 分钟内第二次再封”的方式降低页面首屏误伤；基于当前代码态重新拉起的 `18083` 临时实例已完成完整复核，第一轮 `14` 个真实签名资源的秒级突发访问全部返回 `200`，第二轮同样访问到第 `13`、`14` 个时开始返回 `429`；随后在 IDEA 重启后的 `8080` 上按同样链路复核，也已得到一致结果。
- 运行态复核：`2026-06-03` 已在 `8080` 上确认三类结果与 `18080` 一致：
  - 长窗口残留但近 2 分钟无突发时，单账号下载返回 `200`
  - 同一 IP/UA 多账号协同下载在第 `25` 个不同资源返回 `429`
  - 同一 IP 多账号协同下载在第 `31` 个不同资源返回 `429`
  - 长时间低抖动顺序拉取在前 `10` 次返回 `200`，第 `11`、`12` 次返回 `429`
  - 秒级 burst 第一轮 `14` 次返回 `200`，第二轮第 `13`、`14` 次返回 `429`
  - `10` 秒内同一 IP 跨多个账号访问不同资源超过 `15` 个时，在第 `17` 次请求返回 `429`
  - `10` 秒内同一 IP/UA 跨多个账号访问不同资源超过 `12` 个时，在第 `14` 次请求返回 `429`
- 再补充：`2026-06-04` 已继续给 IP 级协同规则补“跨项目数”侧信号，默认要求同一出口在命中“同一 IP / 同一 IP+UA 多账号协同”封禁前，还需在窗口内跨到至少 `2` 个项目；目标是降低共享办公 / NAT 场景下多人在同一项目内正常协作时的误伤概率。
- 运行态复核补记：`2026-06-04` 基于当前代码态临时拉起的 `18084` 已验证通过这次 NAT 误伤缓冲：
  - 同一项目、同一出口 IP、多人协作访问 `31` 个不同资源时，全程 `200`
  - 同一项目、同一出口 IP + 同一 UA、多人协作访问 `25` 个不同资源时，全程 `200`
  - 跨项目、同一出口 IP、多人协作访问时，第 `19` 次开始返回 `429`
  - 跨项目、同一出口 IP + 同一 UA、多人协作访问时，第 `15` 次开始返回 `429`
- 运行态复核再补记：`2026-06-04` 晚间对 IDEA 当前 `8080` 做黑盒复核时，发现它仍表现为“旧行为”，说明运行中的后端还没有真正切到这版 NAT 缓冲代码：
  - 在清空这批临时测试账号的历史风险键后，同一项目、同一出口 IP、3 个账号轮转访问 `31` 个不同资源，前 `18` 次返回 `200`，第 `19` 次开始返回 `429`
  - 对应命中原因仍是 `10 秒内同一 IP 跨多个账号访问不同资源超过 15 个`，没有体现新规则要求的“至少跨 2 个项目”这一保护条件
  - 这意味着“源码态已修正，但 8080 运行态还未真正吃到修正”
- 再补充：`2026-06-04` 深夜已继续把 `/uploads/**` 的上传风控从 `AccessLogFilter` 里的“请求结束后补判”前移到 `UploadDownloadRateLimitFilter`，改为在签名用户 / 项目属性已经就位的当前请求上直接判定并拦截。
- 当前代码态运行实例 `18085` 已完成这次前移改造后的黑盒复核：
  - 同一项目、同一出口 IP、3 个账号轮转访问 `31` 个不同资源，全程 `200`
  - 同一项目、同一出口 IP + 同一 UA、2 个账号轮转访问 `25` 个不同资源，全程 `200`
  - 跨项目、同一出口 IP、3 个账号轮转访问时，第 `16` 次开始返回 `429`
  - 跨项目、同一出口 IP + 同一 UA、2 个账号轮转访问时，第 `13` 次开始返回 `429`
  - 相比之前 `18084` 上“命中后下一跳才封”的表现，这一版已经是“当前触发请求立即返回 `429`”
- 再补充：`2026-06-04` 收尾阶段又继续给一批历史上默认全量返回的小列表接口补了后端硬上限，当前源码已完成 `compile + package`：
  - 已收口：`/api/tasks`、`/api/workflows`、`/api/workflows/{id}/runs`、`/api/quota/records`
  - 已继续收口：`/api/projects`、`/api/teams`、`/api/teams/{id}/members`、`/api/model-providers`、`/api/project-shares/{projectId}`、`/api/video/subtitles/{assetId}`、`/api/api-keys`、`/api/admin/api-keys`、`/api/admin/webhooks`、`/api/community/posts/{id}/comments`、`/api/prompt-templates/{id}/comments`、`/api/style-cards/{id}/comments`、`/api/announcements/active`、`/api/admin/announcements`、`/api/notifications/unread`、`/api/dict/{dictCode}`、`/api/admin/dict`、`/api/admin/dict/{dictId}/items`、`/api/admin/config`、`/api/admin/scheduled-tasks`
  - 这一步主要是在现有行为风控之外，继续降低“单次请求直接扫走一批元数据”的收益
- 再补充：`2026-06-04` 这一轮也继续给 `/export` 类路径补了“当前请求即判定”的导出轨迹风控，当前源码已完成 `compile + package`：
  - 同一主体短窗口内连续导出过多次时，当前请求直接返回统一风险 `429`
  - 同一主体短窗口内连续导出多个不同项目 / 数据视图时，当前请求直接返回统一风险 `429`
  - 同一 IP 或同一 IP/UA 下多个账号 / API Key 短窗口内连续导出多个目标时，当前请求直接返回统一风险 `429`
  - 这一步主要是在原有接口级限流和 canary 之外，继续压缩“低频大包慢慢搬”的合法导出路径
- 运行态补记：`2026-06-04 23:39:23` 已在 IDEA 当前 `8080` 上完成 `/export` 新风控首轮黑盒复核：
  - 创建 `3` 个临时空项目后连续调用 `POST /api/projects/{id}/export`
  - 前 `2` 次返回 `200`
  - 第 `3` 次在当前请求直接返回统一风险 `429`
  - 数据库 `access_log` 对应记录为：`path=/api/projects/66/export`、`status_code=429`、`risk_reason=30 分钟内连续导出多个不同目标`
- 再补充：`2026-06-05` 凌晨用户再次重启 IDEA 后，已经把之前挂着的两块运行态复核补齐到 `8080`：
  - 小列表接口硬上限已完成黑盒确认：公告 `120 -> 100`、字典项 `130 -> 100`、配置 `130 -> 100`、定时任务 `132 -> 100`、评论树 `220 -> 200`、未读通知 `140 -> 100`
  - NAT / 共享办公误伤缓冲也已完成黑盒确认：同一项目、同一出口 IP + 同一 UA、`2` 个账号协作下载 `20` 个不同资源全程 `200`
  - 跨项目、同一出口 IP、`3` 个账号快速交错下载时，第 `23` 次当前请求直接 `429`
  - 跨项目、同一出口 IP + 同一 UA、`2` 个账号快速交错下载时，第 `14` 次当前请求直接 `429`
  - 对应 `access_log` 已分别记录 `10 秒内同一 IP 跨多个账号访问不同资源超过 15 个` 与 `10 秒内同一 IP/UA 跨多个账号访问不同资源超过 12 个`
- 再补充：`2026-06-05` 本轮又继续把社区/模板/风格卡这类“合法大响应接口”的行为链纳入风控：
  - 已新增“公共内容分页扫库 + 详情/评论联动遍历”识别
  - 默认阈值为 `15` 分钟内分页扫描超过 `18` 次，且同时累计到至少 `12` 个不同公共内容目标
  - 详情遍历和评论树遍历也分别有独立阈值，命中后当前请求直接 `429`
  - 基于当前代码态临时拉起的 `18086` 已完成黑盒复核：同一匿名 IP 先访问 `12` 个不同帖子详情，再扫到第 `19` 页时当前请求直接返回 `429`
  - `access_log` 对应记录为：`path=/api/community/posts`、`status_code=429`、`risk_reason=检测到公共内容分页扫库并伴随详情/评论遍历`
- 再补充：`2026-06-05` 本轮又继续给公共内容补了“跨时段慢速公共内容采集”识别：
  - 默认阈值为：`60` 分钟窗口、`5` 分钟时间片、至少 `4` 个活跃时间片、至少 `8` 次分页扫描、至少 `24` 个不同公共内容目标
  - 基于当前代码态临时拉起的 `18090` 已按默认阈值完成黑盒复核：同一匿名 IP 在首个 `5` 分钟时间片内完成 `24` 个不同帖子详情与 `8` 次分页请求后，再分别跨到 `01:25 / 01:30 / 01:35` 三个时间片补分页，到 `01:35` 那一跳当前请求直接返回 `429`
  - `access_log` 对应记录为：`path=/api/community/posts`、`status_code=429`、`risk_reason=检测到跨时段慢速公共内容采集`
- 再补充：`2026-06-05` 本轮又继续给 `/uploads/**` 补了“分段式小批量下载”识别：
  - 默认阈值为：`12` 秒内命中至少 `6` 个不同资源记为一次事件，`15` 分钟内累计到第 `3` 次时当前请求直接 `429`
  - 基于当前代码态临时拉起的 `18087` 已完成黑盒复核：按 `6 + 6 + 6`、批次间隔约 `13` 秒、批内间隔约 `120ms` 的节奏访问真实签名资源时，前 `17` 次返回 `200`，第 `18` 次当前请求直接返回 `429`
  - `access_log` 对应记录为：`path=/uploads/u3p48x5.jpg`、`status_code=429`、`risk_reason=检测到分段式小批量下载行为`
- 再补充：`2026-06-05` 本轮又继续给 `/uploads/**` 补了“跨时段、跨项目的低速分布式资源搬运”识别：
  - 默认阈值为：`60` 分钟窗口、`5` 分钟时间片、至少 `4` 个活跃时间片、至少跨 `3` 个项目、至少访问 `24` 个不同资源
  - 基于当前代码态临时拉起的 `18088` 已按默认阈值完成黑盒复核：在 `00:42-00:55` 之间跨 `4` 个时间片累计访问 `24` 个不同签名资源时，第 `24` 次当前请求直接返回 `429`
  - `access_log` 对应记录为：`path=/uploads/u3p48x3.jpg`、`status_code=429`、`risk_reason=检测到跨时段、跨项目的低速分布式资源搬运`
- 再补充：`2026-06-05` 本轮又继续给 `/uploads/**` 补了“同一 IP/UA 下多账号跨时段慢速协同搬运”识别：
  - 默认阈值为：`60` 分钟窗口、`5` 分钟时间片、至少 `4` 个活跃时间片、至少 `2` 个账号、至少跨 `3` 个项目、至少访问 `24` 个不同资源
  - 基于当前代码态临时拉起的 `18089` 已按默认阈值完成黑盒复核：`2` 个临时测试账号在同一 `IP + UA` 下，跨 `3` 个项目、`4` 个时间片累计访问第 `24` 个不同签名资源时，当前请求直接返回 `429`
  - `access_log` 对应记录为：`path=/uploads/u2p47x0.jpg`、`status_code=429`、`risk_reason=检测到同一 IP/UA 下多账号跨时段慢速协同搬运`
- 再补充：`2026-06-05` 本轮又继续给 `/uploads/**` 补了“跨 IP 池分散协同搬运”识别：
  - 默认阈值为：`60` 分钟窗口、`5` 分钟时间片、至少 `4` 个活跃时间片、至少 `3` 个不同 IP、至少 `2` 个账号、至少跨 `3` 个项目、至少访问 `24` 个不同资源
  - 基于当前代码态临时拉起的 `18091` 已按默认阈值完成黑盒复核：`3` 个临时测试账号在同一 `UA` 下，分布于 `3` 个不同来源 IP，跨 `3` 个项目、`4` 个时间片累计访问后，到 `01:55` 那一跳当前请求直接返回 `429`
  - `access_log` 对应记录为：`path=/uploads/u3p48x7.jpg`、`status_code=429`、`risk_reason=检测到跨 IP 池分散协同搬运`
- 运行态再补记：`2026-06-05 02:24` 已完成 `8080` 运行态接管：
  - 旧的 `8080 / 8081` Java 进程已停掉，并重新用最新打包产物拉起 `PID 29924`
  - 新 `8080` 进程启动时间为 `2026-06-05 02:24:32`
  - 登录、验证码、共享资产库冒烟已通过
  - `18087` 已在真实 `8080` 上补通黑盒：同一来源 IP 按 `6 + 6 + 6` 节奏访问真实签名资源时，前 `17` 次返回 `200`，第 `18` 次当前请求直接 `429`
  - `access_log` 对应记录为：`path=/uploads/u3p48x5.jpg`、`status_code=429`、`risk_reason=检测到分段式小批量下载行为`
- 运行态再补记：`2026-06-05 03:42-04:00` 已继续用 `backend/scripts/runtime-risk-verify.cjs` 把剩余 `5` 条最新规则的真实 `8080` 黑盒全部补齐：
  - `18086`：`page=19` 当前请求直接 `429`，`access_log.id=8905`
  - `18090`：第 `4` 个时间片访问 `page=11` 时当前请求直接 `429`，`access_log.id=9018`
  - `18088`：同一签名用户跨 `3` 项目、`4` 个时间片累计后，在第 `4` 个时间片访问 `/uploads/u1p46x0.jpg` 时当前请求直接 `429`，`access_log.id=9019`
  - `18089`：同一 `IP + UA` 下多账号跨时段协同时，在第 `4` 个时间片访问 `/uploads/u1p46x0.jpg` 时当前请求直接 `429`，`access_log.id=9020`
  - `18091`：同一 `UA` 下跨 `3` 个不同来源 IP 协同时，在第 `4` 个时间片访问 `/uploads/u1p46x0.jpg` 时当前请求直接 `429`，`access_log.id=9021`
  - 到这里，本轮新增规则已经完成“代码态 + 真实 `8080` 运行态”双重闭环，当前评估范围内可以按 `10 / 10` 记账

## P0 - 已完成

- 已给读取型 API 接入默认 `IP` + `USER_API` 双维度限流，并对导出和 `/uploads/**` 资产下载增加更严格限流。
- 已给 API Key 增加独立风控：scope、过期时间、每日额度、最后使用 IP / UA 记录、异常调用冻结。
- 已增加可信代理真实 IP 解析，后端仅在 remoteAddr 命中可信代理时读取 `X-Real-IP` / `X-Forwarded-For`。

## P1 - 已完成

- 已将前端静态签名密钥改为服务端短期 challenge 机制，每次敏感写请求使用一次性 challenge 验签。
- 已对高价值接口增加异常行为审计：高频读取、连续导出、API Key 多 IP / UA 变化、固定间隔访问。
- 已对触发风险的账号/API Key 临时降速，并支持通过 `X-Risk-Captcha-Ticket` 二次验证码解除风险标记。

## P2 - 已完成

- 已增加访问日志聚合面板，展示 IP、用户、API Key、接口、状态码、耗时和限流命中情况。
- 已给项目导出和 CSV 导出加入可追踪 canary 字段，便于定位批量搬运来源。

## P3 - 下一轮优先

- `18080` 的“真实文件存在”场景完整复核已经完成；`18086 / 18088 / 18089 / 18090 / 18091` 的 `8080` 运行态也已经补齐，后续新增规则继续沿用 `backend/scripts/runtime-risk-verify.cjs` 这套脚本化黑盒方式即可。
- 继续观察共享办公 / NAT 场景下的误伤边界，重点复核“同一出口、同一项目、多人正常协作”是否仍会命中长窗口或 `10` 秒级协同限制。
- 下次继续前，先确认实际对外提供服务的是哪一个后端实例；`18084` 与 `8080` 共用同一套数据库访问日志，黑盒复核时需要用测试时间段和测试 IP / UA 明确区分实例来源。
- 下一批更值得继续做的，不再是补本轮遗留，而是往下一档对抗能力推进：
  - 继续观察更弱终端指纹、不同浏览器指纹伪装、代理池轮换下的人群级协同行为
  - 为导出、下载、公共内容这三条主链补更稳定的“运行态一键复核脚本 + 报告归档”流程，减少后续人工排查成本
